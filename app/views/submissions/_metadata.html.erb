<dl>
  <dt><%= submission.title %></dt>
  <% Submission::METADATA_FIELDS.each do |fieldname| %>
    <% if submission.send(fieldname.to_sym).present? %>
      <dt><%= t(fieldname) %></dt>
      <dd><%= submission.send(fieldname.to_sym) %></dd>
    <% end %>
  <% end %>
</dl>
